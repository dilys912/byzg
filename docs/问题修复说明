宝钢问题修复说明：

问题号67：
   #问题描述：依据生产订单做了下线入库单，后又删除，不能再依据该生产订单下线？
   #测试问题：依据生产订单做了下线检验单-》保存后，下线检验单点击确认后，自动生成产成品入库单，
	，之后再删除对应的产成品入库单跟下线检验单后，重新做下线检验单时，仍然能够根据之前的生产订
	单重新做一个下线检验单，因此，该问题不存在。

问题号8：
	登录到2013年11月20，进入销售出库，然后查询单据号为：XC1311110153 的销售出库单，签字-》生成销售成本结转
	取消签字：系统会自动删除销售成本结转
	需实现功能：（代加工流程的） 通过销售出库单的跺号/存货 找到采购到货单，然后取到采购到货单单价，带入到
	销售成本结转单中
	销售成本结转：财务会计-》存货核算-》出库业务-》销售成本结转
	测试备注：（销售出库单查询条件选择：2014-03-25 ，单据状态：全部  ，入库退库：出库。查询出数据后，如果已经签字，先取消签字，然后点击签字按钮，
		签字完成后，点击联查按钮，联查销售成本结转，查看销售成本结转界面的单价是否等于采购到货单的单价，（根据销售出库单的存货编码、跺号查询 采购到货单
			看单价是否一致即可））
	补丁生效前提：1.进入集团下功能注册-》找到销售成本结转按钮，增加一个“自动询价”按钮

问题号56：
	#问题描述：应收与收款单、应付单与付款单核销后，期初应收款和应付款余额出差错，应纠错
	#测试问题：此问题，财务表示，很少用，问题目前没有复现，先不处理
	
问题号41：
	#问题描述：产成品下线时跳号及乱码不能及时提示，导致出错率较高（判断条件：前一个单件号顺序加1，如果不是就提示）
	#测试问题：	新增一个“垛号排序”按钮，界面数据录入好之后，点击此按钮，可将界面的所有数据按跺号升序排列，在点击保存按钮后，可准确指定出是哪行跳号。
				修改类：	nc.ui.mo.mo6600.MyEventHandler
						nc.impl.mm.mm6600.IMo6600Impl，需要重启服务
	
问题号55：
	#问题描述：库存账龄分析表(至少包含主要原材料及备品备件,产成品,现有的报表数据不对)  如果数据结果不符合产品本身设计逻辑，则为BUG。如果之前逻辑与新系统逻辑不一致，则为新需求，不在需求清单中，不做处理
	#修改地方：	1.修改库龄查询模板：新增“存货分类”，“货位/货架”，“日期”
				2.视图改造：在视图“V_IC_ONHANDNUM4”中关联 "bd_invbasdoc","bd_invcl" 查询出 “invclasscode”字段，供外界使用
				3、库存账龄分析查询模板新增三个字段
				~存货分类-》该字段由改造后的视图取得： “invclasscode” 返回类型：5
				~货位/货架-》查询模板新增字段：“cscode” 返回类型：5
				~日期-》直接使用dbizdate，查询模板上如果该条件未选，则此字段的值为当前系统登录日期，如果选择了则根据实际选择的时间来：“dbizdate”
				修改代码备注：ic/private->nc.bs.ic.ic637.StockAgeDMO
							ic/clienti->nc.ui.ic.ic637.ClientUI
							
		2014-03-28修改内容：
			库龄报表新增“货位编码”、“货位名称”字段。
			实现方式：1.进入集团下修改库龄报表模板，新增“(hwcode->排列顺序26)货位编码”、“(hwname排列顺序27)货位名称”字段,
					(节点编码：40083414)
					 2.
	
	
	
	
	
	
